obligations:
  - code: AZ_VAT_MONTHLY
    title: VAT monthly return
    description: Monthly VAT return (demo)
    country_code: AZ
    rules:
      - regime: monthly
        rrule: {}
        due_day: 20
        due_shift: next_business_day
        grace_days: 0
        holiday_calendar_code: AZ
        is_active: true

  - code: AZ_VAT_QUARTERLY
    title: VAT quarterly return
    description: Quarterly VAT return (demo)
    country_code: AZ
    rules:
      - regime: quarterly
        rrule: {}
        due_day: 20
        due_shift: next_business_day
        grace_days: 0
        holiday_calendar_code: AZ
        is_active: true

  - code: AZ_PAYROLL_MONTHLY
    title: Payroll & social contributions
    description: Monthly payroll/social contributions (demo)
    country_code: AZ
    rules:
      - regime: monthly
        rrule: {}
        due_day: 15
        due_shift: next_business_day
        grace_days: 0
        holiday_calendar_code: AZ
        is_active: true
